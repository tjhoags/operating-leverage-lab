export function Footer() {
  return (
    <footer class="footer">
      <div>
        <strong>What this is</strong>A scenario model. It turns explicit assumptions into a cash ledger, an hours ledger and an
        optional conditional comparison. It does not observe or verify anything.
      </div>
      <div>
        <strong>Where your data goes</strong>Nowhere. The page runs entirely in your browser. Your scenario is kept in this
        browser's local storage and leaves only when you download it as a file.
      </div>
      <div>
        <strong>Examples</strong>The three worked examples are fictional. Their figures are recomputed from their inputs each
        time the page loads and are not customer results.
      </div>
    </footer>
  );
}
